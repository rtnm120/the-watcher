[project]
name = "the_watcher"
version = "0.1.0"
description = "A simple discord bot that notifies users when Lost Ark server maintenance has concluded"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "discord-py>=2.7.1",
    "dotenv>=0.9.9",
]
